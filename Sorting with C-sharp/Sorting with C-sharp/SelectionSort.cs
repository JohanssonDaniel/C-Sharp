namespace Sorting_with_C_sharp
{
    class SelectionSort
    {
        public static int[] Sort(int[] list) {
            //int i, j;
            int n = list.Length;
            int[] t = { 1 };
            //for (i = 0; i < n - 1; i++)
            //{
            //    int iMin = i;
            //    for (j = 0; j < n; j++)
            //    {
            //        if (list[j] < list[iMin])
            //        {
            //            iMin = j;
            //        }
            //    }
            //    if (iMin != j)
            //    {
            //        int temp = list[j];
            //        list[j] = list[iMin];
            //        list[iMin] = list[j]; 
            //    }
            //}
            //return list;
            for (int i = 0; i < n; i++)
            {
                
            }
            System.Console.WriteLine(i);
            return t;
        }
    }
}
